terraform:
  backend_type: azurerm
  backend:
    azurerm:
      subscription_id: var.subscription_id
      tenant_id: var.tenant_id
      container_name: state-tf-poc
      storage_account_name: storagetfpoc
      resource_group_name: rg-tf-poc
      key: terraform.tfstate
